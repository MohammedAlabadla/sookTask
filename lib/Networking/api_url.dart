class ApiUrl {
  //
  static const _baseUrl = 'https://api.github.com';

  // api's
  static var search = '$_baseUrl/search/repositories';
  static var fetchRepo = '$_baseUrl/orgs/google/repos';

//
}
